import HeroSection from '../components/HeroSection';
import Layout from '../components/Layout';

const Home = () => {
  return (
    <Layout>
      <HeroSection />
    </Layout>
  );
};

export default Home;
